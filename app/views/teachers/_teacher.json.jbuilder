json.extract! teacher, :id, :first_name, :last_name, :gender, :date_of_birth, :mobile_number, :email, :listening_skills_jaws, :writing_skills_with_jaws, :strengths, :weaknesses, :tirms, :user_id, :created_at, :updated_at
json.url teacher_url(teacher, format: :json)
